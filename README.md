# תקציב הבית 🏠

אפליקציה לניהול כלכלת משק הבית: הכנסות, הוצאות קבועות ושוטפות, חלוקה בין חשבון פרטי של **יולי**, חשבון פרטי של **נעמה** וחשבון **משותף**, וסיכום כולל של כל מה שנכנס ויוצא.

האפליקציה עובדת ישירות בדפדפן, ללא שרת וללא הרשמה. הנתונים נשמרים מקומית במכשיר (`localStorage`), וניתן לייצא ולייבא אותם כקובץ גיבוי.

## איך פותחים

**הדרך הקצרה – קישור בדפדפן.** האפליקציה מפורסמת כעמוד באתר claude.ai:
<https://claude.ai/artifact/5XxRpRkiA1MW6E1Z5Wwe8i>. פותחים בטלפון או במחשב, אפשר
להוסיף למסך הבית. בגרסה הזו הנתונים נשמרים באחסון המשותף של האפליקציה, כך שכל מי
שפותח את הקישור רואה את אותם נתונים – בכל מכשיר. כשהסנכרון פעיל מופיע הסימון
"מסונכרן" בראש העמוד.

**הרצה מקומית** – למי שרוצה להריץ מהקוד (דורש Node.js 20 ומעלה):

## הפעלה

```bash
npm install
npm run dev        # סביבת פיתוח (http://localhost:5290)
npm run build      # בנייה לפרודקשן לתיקיית dist
npm run preview    # תצוגה מקדימה של הבנייה (http://localhost:4290)
npm run build:artifact  # בנייה + הכנת עמוד יחיד לפרסום כ-Artifact
npm test           # בדיקות לחישוב ההתחשבנות
```

הפורטים נקבעים ב-`vite.config.ts` (5290 לפיתוח, 4290 לתצוגה מקדימה) – הרחק
מברירות המחדל של Vite שנוטות להיות תפוסות. אם גם הפורט הזה תפוס, Vite עובר לבד
לפורט הפנוי הבא ומדפיס את הכתובת. לפורט חד-פעמי אחר:

```bash
npm run dev -- --port 5400
```

הבנייה יוצרת קבצים סטטיים בלבד (`base: './'`), כך שאפשר להעלות את תוכן `dist/` לכל אחסון סטטי – GitHub Pages, Netlify, או אפילו תיקייה משותפת.

## מה יש באפליקציה

| מסך | מה הוא עושה |
| --- | --- |
| **סקירה** | מאזן החודש כמספר-על, הכנסות מול הוצאות ב-12 החודשים האחרונים, מאיפה יצא הכסף, מי הכניס כמה, הקטגוריות הגדולות, מעקב תקציב ותשלומים שעוד צפויים |
| **תזרים חודשי** | כל השורות של החודש – קבועות וחד-פעמיות – עם סימון "בוצע", דילוג על חיוב ועדכון הסכום שנגבה בפועל |
| **קבועות** | שכר דירה, ועד בית, ארנונה, חשמל, מים, גז, גן, צהרון, חוגים, מנויים, ביטוחים, משכורות והעברות קבועות לחשבון המשותף – כולל ממוצע חודשי ועלות שנתית |
| **יומן תנועות** | התנועות החד-פעמיות, מקובצות לפי חודש, עם חיפוש וסינון |
| **חשבונות** | יתרה נוכחית ומגמה לכל חשבון, תנועות החודש, והעברות בין החשבונות |
| **קטגוריות** | הוספה ועריכה של קטגוריות – שם, קבוצה, סמל וצבע – עם ההוצאה בחודש הנוכחי מול ממוצע 3 חודשים |
| **דוחות והתחשבנות** | סיכומים לחודש הנוכחי או ל-3 / 6 / 12 חודשים, פילוח לפי קבוצה, קטגוריה, חשבון ואדם, התחשבנות הוגנת בין בני הבית עם סימון "הוסדר", וייצוא ל-CSV |
| **הגדרות** | שמות בני הבית, צבע הזהות של כל אחד והצבע הראשי, מטבע, מראה בהיר/כהה, שיטת החלוקה בהתחשבנות, גיבוי ושחזור |

## איך מסומן מאיפה יצא הכסף

לכל תנועה יש **חשבון מקור**: החשבון הפרטי של יולי, החשבון הפרטי של נעמה, או החשבון המשותף. כל חשבון משויך לבעלים, ולבעלים יש צבע קבוע שמלווה אותו בכל המסכים – בטבלאות, בגרפים ובסיכומים. כך אפשר לראות בכל רגע כמה יצא מכל כיס.

הכנסות מסומנות פעמיים: לאיזה חשבון הן נכנסו, ו**של מי** הן (יולי / נעמה / משותף, למשל קצבת ילדים). זה מה שמאפשר את החלוקה היחסית בהתחשבנות.

## מה מתחלק ומה אישי

לא כל הוצאה צריכה להתחלק חצי-חצי. לכל הוצאה יש סימון:

* **משותפת** (ברירת המחדל) – נכללת באיזון ומתחלקת לפי שיטת החלוקה שנבחרה.
* **אישית** – מי שההוצאה שלו נושא בה לבד, והיא לא נכללת באיזון כלל.

את הסימון אפשר לקבוע בשלוש דרכים:

1. **בטופס** של תנועה או של חיוב קבוע: שדה "מתחלק באיזון?".
2. **בקטגוריה שלמה**: סימון "קטגוריה אישית" – כל ההוצאות בה אישיות כברירת מחדל
   (בנתוני הדוגמה: מנויים דיגיטליים, חדר כושר, הוצאות אישיות). סימון בתנועה בודדת
   תמיד גובר על הגדרת הקטגוריה.
3. **ישר מהתזרים החודשי**: כפתור "לאישי" / "למשותף" בכל שורה. בשורה של חיוב קבוע
   השינוי חל על כל החודשים.

הוצאה אישית ששולמה מ**חשבון פרטי** מיוחסת אוטומטית לבעל החשבון. הוצאה אישית
ששולמה מה**חשבון המשותף** צריכה שיוך מפורש – האפליקציה תשאל של מי היא, ועד שייבחר
מישהו היא תסומן "אישי · חסר שיוך" ותמשיך להיכלל באיזון. הוצאה אישית שיצאה מהקופה
המשותפת נחשבת משיכה אישית ממנה, ומקטינה את חלקו של מי שהיא שלו במימון ההוצאות
המשותפות – כך שהאיזון יוצא נכון בדיוק.

## איך עובדת ההתחשבנות

המטרה: לענות על השאלה "מי שילם יותר מחלקו החודש".

1. **מה כל אחד נשא בפועל** = ההוצאות המשותפות ששולמו מהחשבון הפרטי שלו + חלקו בהוצאות המשותפות שיצאו מהחשבון המשותף. החלק בחשבון המשותף נקבע לפי חלקו במימון החשבון (העברות קבועות + הכנסות שנכנסו ישירות למשותף), כך שאין ספירה כפולה.
2. **החלק ההוגן** מחושב על ההוצאות המשותפות בלבד, לפי שיטת החלוקה שנבחרה בהגדרות:
   * חלוקה שווה (50/50),
   * חלוקה יחסית להכנסות (ברירת המחדל),
   * חלוקה מותאמת באחוזים.
3. **המאזן** = מה שנשא פחות החלק ההוגן. מאזן חיובי = שילם מעבר לחלקו, והאפליקציה מציעה בדיוק כמה כסף להעביר ולמי.

אחרי שההעברה מתבצעת בפועל אפשר לסמן **"סימון כהוסדר"**: נשמר רישום עם התאריך
והסכום, והתקופה מסומנת כ"הוסדר" במקום "טרם הוסדר". אפשר גם לרשום באותה פעולה את
ההעברה עצמה בין החשבונות הפרטיים, כך שהיתרות יישארו מדויקות – ההעברה הזו אינה
משפיעה על חישוב החלוקה, רק על יתרות החשבונות. ביטול הסימון מסיר גם את ההעברה
שנרשמה. איזונים קודמים מוצגים בטבלה "איזונים קודמים".

## תזכורת שבועית

הוגדרה תזכורת קבועה שנשלחת במייל בכל יום חמישי ב-19:00 (שעון ישראל) – להזין את
ההוצאות של השבוע שעבר. אפשר לשנות או לבטל אותה במסך ה-Routines של claude.ai.

## תדירויות נתמכות

חודשי, דו-חודשי (ארנונה, חשמל, מים), רבעוני, חצי שנתי, שנתי (טסט לרכב, ביטוחים) ושבועי. חיובים שאינם חודשיים מוצגים גם כ"ממוצע לחודש", כדי שתמונת ההוצאות החודשית תהיה אמיתית.

## הנתונים שלכם

* בגרסה המפורסמת הנתונים נשמרים באחסון המשותף של האפליקציה ומסונכרנים בין כל מי
  שפותח את הקישור. בהרצה מקומית הכול נשמר בדפדפן של המכשיר בלבד.
* במסך ההגדרות אפשר **לייצא גיבוי JSON** ולטעון אותו במכשיר אחר (כך גם משתפים את אותו מצב בין שני בני הזוג).
* כפתור **"התחלה מאפס"** מוחק את נתוני הדוגמה ומאפשר להזין נתונים אמיתיים. כפתור **"איפוס לנתוני דוגמה"** מחזיר את המצב ההתחלתי.
* האפליקציה מגיעה עם נתוני דוגמה מלאים (שכר דירה, גן, מנויים, משכורות, העברות לחשבון המשותף ועוד) כדי שאפשר יהיה להתרשם מכל המסכים מיד – מומלץ להחליף אותם בנתונים שלכם.

## מבנה הפרויקט

```
src/
  types.ts              מודל הנתונים
  data/seed.ts          נתוני הדוגמה
  lib/
    compute.ts          מנוע החישוב: מימוש תנועות קבועות, סיכומים, יתרות, התחשבנות
    settle.test.ts      בדיקות לחישוב ההתחשבנות (npm test)
    dates.ts            עזרי תאריכים וחודשים
    money.ts            עיצוב סכומים
    store.tsx           ניהול מצב + שמירה מקומית
    colors.ts           צבעי זהות לבעלי החשבונות
  components/
    ui.tsx              רכיבי בסיס (כרטיס, אריח נתון, מודאל, שדה)
    charts.tsx          גרפים (עמודות, דירוג, חלק-מתוך-שלם, מגמה)
    forms.tsx           טפסים לתנועות, חשבונות וקטגוריות
  pages/                מסכי האפליקציה
```

## צבעים

לכל בן בית יש צבע זהות שמלווה אותו בכל המסכים – בטבלאות, בגרפים ובסיכומים. הצבעים
ניתנים לשינוי במסך ההגדרות. בנוסף אפשר לתת צבע משלו לכל חשבון (במסך החשבונות ←
עריכה) ולכל קטגוריה (בעת ההוספה או העריכה). חשבון ללא צבע משלו יורש את הצבע של
בעליו.

לוח הצבעים המוצע (וכן צבעי ברירת המחדל) נבחר מפלטה שעברה אימות ניגודיות והבחנה
בעיוורון צבעים בשני מצבי התצוגה. צבעי ברירת המחדל מתאימים את עצמם למצב בהיר וכהה;
צבע שנבחר ידנית נשאר זהה בשניהם. בנוסף אפשר לבחור צבע חופשי לגמרי.
